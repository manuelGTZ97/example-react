import React, { Component } from 'react';

function NotFound(){
        return(
            <div>
                <h1>Pagina no encontrada je.</h1>
            </div>
        );
}

export default NotFound;