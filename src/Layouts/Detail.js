import React, { Component } from 'react';
import Models from '../CssComponents/Models';

export default class Detail extends Component {
    render(){
        return(
            <div style={margin}>
                <Models />
            </div>
        );
    }
}

const margin ={
    'margin-top' : '20px'
};
